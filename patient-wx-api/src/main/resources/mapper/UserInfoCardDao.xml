<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.gas.patient.wx.api.db.dao.UserInfoCardDao">
    <select id="searchUserTel" parameterType="int" resultType="String">
        SELECT tel
        FROM PATIENT_USER_INFO_CARD
        WHERE user_id = #{userId}
    </select>

    <select id="searchByUserId" parameterType="int" resultType="com.example.gas.patient.wx.api.db.pojo.UserInfoCardEntity">
        SELECT id, user_id, uuid, name, sex, pid, tel, birthday, medical_history, insurance_type, exist_face_model, worker_id
        FROM PATIENT_USER_INFO_CARD
        WHERE user_id = #{userId}
    </select>

    <insert id="insert" parameterType="com.example.gas.patient.wx.api.db.pojo.UserInfoCardEntity">
        INSERT INTO PATIENT_USER_INFO_CARD(
            user_id, uuid, name, sex, pid,
            tel, birthday, medical_history, insurance_type, worker_id
        )
        VALUES (
            #{userId}, #{uuid}, #{name}, #{sex}, #{pid},
            #{tel}, #{birthday}, #{medicalHistory}, #{insuranceType}, #{workerId}
        )
    </insert>
</mapper>
