# 智能燃气平台管理系统功能测试文档

**版本：V2.0**  
**日期：2026年4月29日**

---

## 一、测试流程总览

```
Web管理后台（基础数据准备）
    ↓
微信小程序（巡检人员操作）
    ↓
Web管理后台（数据查看验证）
```

---

## 二、测试账号速查表

### Web管理后台

| 角色 | 用户名 | 密码 | 用途 |
|------|--------|------|------|
| 超级管理员 | admin | abc123456 | 全部权限 |
| 测试用户 | testuser | abc123456 | 权限验证 |

### 微信小程序

| 操作 | 说明 |
|------|------|
| 登录 | 点击【注册/登录】→ 微信授权 |
| 实名登记 | 填写姓名、身份证、手机号 |

---

# 第一部分：Web管理后台

---

## 一、部门管理

### 【登录】Web管理后台

```
地址：http://localhost/（或部署地址）
用户名：admin
密码：abc123456
```

> **涉及代码位置**
> - 前端页面：`gas-vue/src/views/login.vue` → `login()` 方法
> - 后端接口：`gas-api/.../controller/MisUserController.java` → `POST /mis_user/login`
> - 权限认证：Sa-Token 框架 `StpUtil.login(userId)`

**核心代码 - 前端登录方法：**
```javascript
// gas-vue/src/views/login.vue
login: function() {
    let that = this;
    if (!isUsername(that.username)) {
        ElMessage({ message: '用户名格式不正确', type: 'error', duration: 1200 });
    } else if (!isPassword(that.password)) {
        ElMessage({ message: '密码格式不正确', type: 'error', duration: 1200 });
    } else {
        let data = { username: that.username, password: that.password };
        that.$http('/mis_user/login', 'POST', data, true, function(resp) {
            if (resp.result) {
                localStorage.setItem('permissions', JSON.stringify(resp.permissions));
                localStorage.setItem('token', resp.token);
                localStorage.setItem('user', JSON.stringify(resp.user));
                router.push({ name: 'Home' });
            } else {
                ElMessage({ message: '登陆失败', type: 'error', duration: 1200 });
            }
        });
    }
}
```

**核心代码 - 后端登录接口：**
```java
// gas-api/.../controller/MisUserController.java
@PostMapping("/login")
public R login(@RequestBody @Valid LoginForm form) {
    Map param = BeanUtil.beanToMap(form);
    Integer userId = misUserService.login(param);
    if (userId != null) {
        StpUtil.login(userId);
        String token = StpUtil.getTokenValue();
        List<String> permissions = StpUtil.getPermissionList();
        MisUserEntity user = misUserService.searchUserById(userId);
        return R.ok().put("result", true).put("token", token)
                .put("permissions", permissions).put("user", user);
    }
    return R.ok().put("result", false);
}
```

---

### 【新增】部门

| 字段 | 输入值 |
|------|--------|
| 部门名称 | 测试部门 |

**操作步骤：**
1. 登录系统
2. 点击左侧菜单【部门管理】
3. 点击【新增】按钮
4. 输入部门名称：`测试部门`
5. 点击【确定】保存

> **涉及代码位置**
> - 前端列表页：`gas-vue/src/views/dept.vue` → `addHandle()` / `loadDataList()`
> - 前端弹窗页：`gas-vue/src/views/dept-add-or-update.vue` → `dataFormSubmit()`
> - 后端接口：`gas-api/.../controller/MisDeptController.java` → `POST /mis_dept/save`
> - 后端Service：`gas-api/.../service/MisDeptService.java`
> - 后端Dao：`gas-api/.../db/dao/MisDeptDao.java` + `mapper/MisDeptDao.xml`

**核心代码 - 前端提交方法：**
```javascript
// gas-vue/src/views/dept-add-or-update.vue
dataFormSubmit() {
    this.$refs.dataForm.validate((valid) => {
        if (valid) {
            let that = this;
            that.loading = true;
            let url = this.dataForm.id ? '/mis_dept/update' : '/mis_dept/save';
            that.$http(url, 'POST', this.dataForm, true, function(resp) {
                that.loading = false;
                if (resp.code === 0 || resp.code === 200) {
                    that.$message.success('操作成功');
                    that.visible = false;
                    that.$emit('refresh');
                } else {
                    that.$message.error(resp.msg || '操作失败');
                }
            });
        }
    });
}
```

**核心代码 - 后端新增部门接口：**
```java
// gas-api/.../controller/MisDeptController.java
@PostMapping("/save")
public R save(@RequestBody MisDeptEntity dept) {
    if (!StpUtil.hasRole("ROOT") && !StpUtil.hasPermission("DEPT:INSERT")) {
        return R.error().put("msg", "无权限访问");
    }
    Integer count = misDeptService.checkDeptName(dept.getName(), null);
    if (count > 0) {
        return R.error().put("msg", "部门名称已存在");
    }
    Integer nextId = misDeptService.getNextId();
    dept.setId(nextId);
    int result = misDeptService.insertDept(dept);
    if (result > 0) {
        return R.ok().put("id", dept.getId());
    } else {
        return R.error();
    }
}
```

---

## 二、科室管理

### 【新增】科室

| 字段 | 输入值 |
|------|--------|
| 科室名称 | 巡检一科 |
| 简介 | 负责区域A巡检工作 |

**操作步骤：**
1. 点击左侧菜单【科室管理】
2. 点击【新增】按钮
3. 输入科室名称：`巡检一科`
4. 输入简介：`负责区域A巡检工作`
5. 点击【确定】保存

> **涉及代码位置**
> - 前端列表页：`gas-vue/src/views/medical_dept.vue` → `loadDataList()`（模板已搭建，methods待实现）
> - 前端弹窗页：`gas-vue/src/views/medical_dept-add-or-update.vue` → `dataFormSubmit()`（模板已搭建，methods待实现）
> - 后端接口：`gas-api/.../controller/MedicalDeptController.java` → `GET /medical_dept/searchAll`
> - 后端Service：`gas-api/.../service/MedicalDeptService.java`
> - 后端Dao：`gas-api/.../db/dao/MedicalDeptDao.java` + `mapper/MedicalDeptDao.xml`

**核心代码 - 后端查询科室接口：**
```java
// gas-api/.../controller/MedicalDeptController.java
@GetMapping("/searchAll")
@SaCheckLogin
public R searchAll() {
    ArrayList<HashMap> list = medicalDeptService.searchAll();
    return R.ok().put("result", list);
}
```

---

## 三、班组管理

### 【新增】班组

| 字段 | 输入值 |
|------|--------|
| 班组名称 | 巡检一组 |
| 所属科室 | 巡检一科 |
| 位置 | 办公楼1楼 |

**操作步骤：**
1. 点击左侧菜单【班组管理】
2. 点击【新增】按钮
3. 选择所属科室：`巡检一科`
4. 输入班组名称：`巡检一组`
5. 输入位置：`办公楼1楼`
6. 点击【确定】保存

> **涉及代码位置**
> - 前端列表页：`gas-vue/src/views/medical_dept_sub.vue` → `loadDataList()`（模板已搭建，methods待实现）
> - 前端弹窗页：`gas-vue/src/views/medical_dept_sub-add-or-update.vue` → `dataFormSubmit()`（模板已搭建，methods待实现）
> - 后端接口：`gas-api/.../controller/MedicalDeptSubController.java` → `GET /medical_dept_sub/searchByDeptId`
> - 后端Service：`gas-api/.../service/MedicalDeptSubService.java`
> - 后端Dao：`gas-api/.../db/dao/MedicalDeptSubDao.java` + `mapper/MedicalDeptSubDao.xml`

**核心代码 - 后端根据科室查询班组接口：**
```java
// gas-api/.../controller/MedicalDeptSubController.java
@GetMapping("/searchByDeptId")
@SaCheckLogin
public R searchByDeptId(@RequestParam("deptId") Integer deptId) {
    ArrayList<HashMap> list = medicalDeptSubService.searchByDeptId(deptId);
    return R.ok().put("result", list);
}
```

---

## 四、角色管理

### 【新增】角色

| 字段 | 输入值 |
|------|--------|
| 角色名称 | 测试角色 |
| 角色描述 | 用于测试的角色 |
| 权限分配 | 用户管理增删改查 |

**操作步骤：**
1. 点击左侧菜单【角色管理】
2. 点击【新增】按钮
3. 输入角色名称：`测试角色`
4. 输入角色描述：`用于测试的角色`
5. 从左侧权限列表选择以下权限，点击→移至右侧：
   - `用户管理添加`
   - `用户管理删除`
   - `用户管理修改`
   - `用户管理查询`
6. 点击【确定】保存

> **涉及代码位置**
> - 前端列表页：`gas-vue/src/views/role.vue` → `addHandle()` / `loadDataList()`
> - 前端弹窗页：`gas-vue/src/views/role-add-or-update.vue` → `dataFormSubmit()` / `getAllPermissions()` / `moveToRight()` / `moveToLeft()`
> - 后端接口：`gas-api/.../controller/MisRoleController.java` → `POST /role/save` + `POST /role/save-permissions`
> - 后端Service：`gas-api/.../service/MisRoleService.java`
> - 后端Dao：`gas-api/.../db/dao/MisRoleDao.java` + `mapper/MisRoleDao.xml`

**核心代码 - 前端两步提交方法（角色信息+权限分配）：**
```javascript
// gas-vue/src/views/role-add-or-update.vue
dataFormSubmit() {
    this.$refs.dataForm.validate((valid) => {
        if (valid) {
            let that = this;
            that.loading = true;
            let url = this.dataForm.id ? '/role/update' : '/role/save';
            that.$http(url, 'POST', this.dataForm, true, function(resp) {
                if (resp.code === 0 || resp.code === 200) {
                    const roleId = that.dataForm.id || resp.id;
                    const permissions = that.rightPermissions.map(item => item.id);
                    that.$http('/role/save-permissions', 'POST', {
                        roleId: roleId,
                        permissions: permissions
                    }, true, function(resp2) {
                        that.loading = false;
                        if (resp2.code === 0 || resp2.code === 200) {
                            that.$message.success('操作成功');
                            that.visible = false;
                            that.$emit('refresh');
                        } else {
                            that.$message.error(resp2.msg || '权限保存失败');
                        }
                    });
                } else {
                    that.loading = false;
                    that.$message.error(resp.msg || '操作失败');
                }
            });
        }
    });
}
```

**核心代码 - 后端保存角色权限接口：**
```java
// gas-api/.../controller/MisRoleController.java
@PostMapping("/save-permissions")
public R savePermissions(@RequestBody Map<String, Object> params) {
    if (!StpUtil.hasRole("ROOT") && !StpUtil.hasPermission("MIS_ROLE:UPDATE")) {
        return R.error().put("msg", "无权限访问");
    }
    Integer roleId = (Integer) params.get("roleId");
    if (roleId != null && roleId == 0) {
        return R.error().put("msg", "超级管理员拥有所有权限，无需分配");
    }
    List<String> permissions = (List<String>) params.get("permissions");
    if (permissions != null && permissions.contains("ROOT")) {
        return R.error().put("msg", "不能为角色赋予ROOT权限");
    }
    int result = misRoleService.saveRolePermission(roleId, permissions);
    if (result > 0) {
        return R.ok();
    } else {
        return R.error();
    }
}
```

---

## 五、用户管理

### 【新增】用户

| 字段 | 输入值 |
|------|--------|
| 用户名 | testuser |
| 姓名 | 测试用户 |
| 电话 | 13800138000 |
| 邮箱 | test@example.com |
| 性别 | 男 |
| 部门 | 测试部门 |
| 状态 | 在职 |
| 角色 | 测试角色 |

**操作步骤：**
1. 点击左侧菜单【用户管理】
2. 点击【新增】按钮
3. 填写用户名：`testuser`
4. 填写姓名：`测试用户`
5. 填写电话：`13800138000`
6. 填写邮箱：`test@example.com`
7. 选择性别：`男`
8. 选择部门：`测试部门`
9. 选择状态：`在职`
10. 选择角色：`测试角色`
11. 点击【确定】保存

> **涉及代码位置**
> - 前端列表页：`gas-vue/src/views/user.vue` → `addHandle()` / `loadDataList()`
> - 前端弹窗页：`gas-vue/src/views/user-add-or-update.vue` → `dataFormSubmit()` / `getRoleList()` / `getDeptList()` / `getUserRoles()`
> - 后端接口：`gas-api/.../controller/MisUserController.java` → `POST /mis_user/save` + `POST /mis_user/save-roles`
> - 后端Service：`gas-api/.../service/MisUserService.java`
> - 后端Dao：`gas-api/.../db/dao/MisUserDao.java` + `mapper/MisUserDao.xml`

**核心代码 - 前端两步提交方法（用户信息+角色关联）：**
```javascript
// gas-vue/src/views/user-add-or-update.vue
dataFormSubmit() {
    this.$refs.dataForm.validate((valid) => {
        if (valid) {
            let that = this;
            let url = this.dataForm.id ? '/mis_user/update' : '/mis_user/save';
            that.$http(url, 'POST', this.dataForm, true, function(resp) {
                if (resp.code === 0 || resp.code === 200) {
                    if (that.dataForm.roles && that.dataForm.roles.length > 0) {
                        that.$http('/mis_user/save-roles', 'POST', {
                            userId: that.dataForm.id || resp.id,
                            roles: that.dataForm.roles
                        }, true, function(resp) {
                            if (resp.code === 0 || resp.code === 200) {
                                that.$message.success('操作成功');
                                that.visible = false;
                                that.$emit('refresh');
                            } else {
                                that.$message.error(resp.msg || '保存角色失败');
                            }
                        });
                    } else {
                        that.$message.success('操作成功');
                        that.visible = false;
                        that.$emit('refresh');
                    }
                } else {
                    that.$message.error(resp.msg || '操作失败');
                }
            });
        }
    });
}
```

**核心代码 - 后端新增用户接口：**
```java
// gas-api/.../controller/MisUserController.java
@PostMapping("/save")
public R save(@RequestBody @Valid MisUserEntity user) {
    if (!StpUtil.hasRole("ROOT") && !StpUtil.hasPermission("MIS_USER:INSERT")) {
        return R.error().put("msg", "无权限访问");
    }
    Integer nextId = misUserService.getNextId();
    user.setId(nextId);
    int result = misUserService.insertUser(user);
    if (result > 0) {
        return R.ok().put("id", user.getId());
    } else {
        return R.error();
    }
}
```

---

### 【验证】新用户登录

```
退出当前登录
使用新账号登录：
用户名：testuser
密码：abc123456
验证：只能访问用户管理模块
```

> **涉及代码位置**
> - 前端退出登录：`gas-vue/src/views/main.vue` → `logout()` 方法
> - 前端登录页面：`gas-vue/src/views/login.vue` → `login()` 方法
> - 后端退出接口：`gas-api/.../controller/MisUserController.java` → `GET /mis_user/logout`
> - 后端登录接口：`gas-api/.../controller/MisUserController.java` → `POST /mis_user/login`
> - 权限控制：前端路由守卫 `gas-vue/src/router/index.js` + 后端 `StpUtil.getPermissionList()`

**核心代码 - 后端退出登录接口：**
```java
// gas-api/.../controller/MisUserController.java
@GetMapping("/logout")
@SaCheckLogin
public R logout() {
    StpUtil.logout();
    return R.ok();
}
```

---

## 六、巡检人员管理

### 【新增】巡检人员

| 字段 | 输入值 |
|------|--------|
| 姓名 | 张三 |
| 性别 | 男 |
| 身份证号 | 500235199001010001 |
| 出生日期 | 1990-01-01 |
| 电话 | 13900139000 |
| 电子邮箱 | zhangsan@test.com |
| 学历 | 大学 |
| 职位 | 工程师 |
| 毕业学校 | 重庆工程学院 |
| 入职时间 | 2026-01-01 |
| 科室 | 巡检一科 |
| 班组 | 巡检一组 |
| 住址 | 重庆市梁平区 |
| 状态 | 在职 |
| 优先推荐 | 是 |
| 标签描述 | 工作认真、责任心强 |

**操作步骤：**
1. 点击左侧菜单【巡检人员管理】
2. 点击【新增】按钮
3. 填写以上信息
4. 选择科室后，班组下拉框自动加载
5. 点击【确定】保存

**⚠️ 记住巡检人员ID：** 新增成功后，记录该巡检人员的ID（如：`3`），后续微信小程序绑定需要用到。

> **涉及代码位置**
> - 前端列表页：`gas-vue/src/views/worker.vue` → `addHandle()` / `loadDataList()` / `expand()`
> - 前端弹窗页：`gas-vue/src/views/worker-add-or-update.vue` → `dataFormSubmit()` / `getDeptList()` / `getSubList()` / `handleDeptChange()`
> - 后端接口：`gas-api/.../controller/GasWorkerController.java` → `POST /gas_worker/save`
> - 后端Service：`gas-api/.../service/GasWorkerService.java`
> - 后端Dao：`gas-api/.../db/dao/GasWorkerDao.java` + `mapper/GasWorkerDao.xml`
> - 科室班组联动：`GET /medical_dept/searchAll` + `GET /medical_dept_sub/searchByDeptId`

**核心代码 - 前端提交方法（含tag数组转字符串、photo路径处理）：**
```javascript
// gas-vue/src/views/worker-add-or-update.vue
dataFormSubmit() {
    this.$refs.dataForm.validate((valid) => {
        if (valid) {
            let that = this;
            let url = this.dataForm.id ? '/gas_worker/update' : '/gas_worker/save';
            let submitData = { ...this.dataForm };
            submitData.tag = Array.isArray(submitData.tag) ? submitData.tag.join(',') : submitData.tag;
            if (submitData.photo && submitData.photo.startsWith('http')) {
                if (submitData.photo.includes('/worker/')) {
                    submitData.photo = submitData.photo.substring(submitData.photo.indexOf('/worker/') + 7);
                    let separator = submitData.photo.includes('?') ? '?' : '&';
                    if (submitData.photo.includes(separator)) {
                        submitData.photo = submitData.photo.split(separator)[0];
                    }
                }
            }
            that.$http(url, 'POST', submitData, true, function(resp) {
                if (resp.code === 0 || resp.code === 200) {
                    that.$message.success('操作成功');
                    that.visible = false;
                    that.$emit('refresh');
                } else {
                    that.$message.error(resp.msg || '操作失败');
                }
            });
        }
    });
}
```

**核心代码 - 后端新增巡检人员接口（含UUID生成和tag JSON处理）：**
```java
// gas-api/.../controller/GasWorkerController.java
@PostMapping("/save")
@SaCheckLogin
@SaCheckPermission(value = {"ROOT", "GAS_WORKER:INSERT"}, mode = SaMode.OR)
public R save(@RequestBody @Valid InsertGasWorkerForm form) {
    Map param = BeanUtil.beanToMap(form);
    param.put("uuid", IdUtil.simpleUUID().toUpperCase());
    if (form.getTag() != null && !form.getTag().isEmpty()) {
        try {
            String json = JSONUtil.parseArray(form.getTag()).toString();
            param.replace("tag", json);
        } catch (Exception e) {
            String[] tags = form.getTag().split(",");
            String json = JSONUtil.parseArray(tags).toString();
            param.replace("tag", json);
        }
    }
    gasWorkerService.insert(param);
    return R.ok();
}
```

---

## 七、燃气用户管理

### 【新增】燃气用户

| 字段 | 输入值 |
|------|--------|
| 用户户号 | TEST001 |
| 户名 | 李四 |
| 用户类型 | 居民用户 |
| 行政区域 | 重庆市/梁平区/梁山街道 |
| 详细地址 | 人民路100号 |
| 楼栋门牌 | 1栋1单元101 |
| 联系电话 | 13700137000 |
| 证件号码 | 500235199002020002 |
| 状态 | 正常 |

**操作步骤：**
1. 点击左侧菜单【燃气用户管理】
2. 点击【新增】按钮
3. 填写以上信息
4. 可点击【地图选择】在地图上选点
5. 点击【确定】保存

> **涉及代码位置**
> - 前端列表页：`gas-vue/src/views/gas_user.vue` → `addHandle()` / `getDataList()`
> - 前端弹窗页：`gas-vue/src/views/gas_user-add-or-update.vue` → `dataFormSubmit()` / `loadAreaData()` / `openMapSelector()`
> - 后端接口：`gas-api/.../controller/GasUserController.java` → `POST /gas_user/save`
> - 后端Service：`gas-api/.../service/GasUserService.java`
> - 后端Dao：`gas-api/.../db/dao/GasUserDao.java` + `mapper/GasUserDao.xml`
> - 区域数据：`gas-api/.../controller/GasUserController.java` → `GET /gas_user/areaAll`
> - 地图选点：`gas-vue/src/components/AmapSelector.vue` + `gas-api/.../controller/AmapController.java`

**核心代码 - 前端提交方法：**
```javascript
// gas-vue/src/views/gas_user-add-or-update.vue
dataFormSubmit() {
    this.$refs.dataForm.validate(valid => {
        if (valid) {
            const url = this.dataForm.id ? '/gas_user/update' : '/gas_user/save';
            this.$http(url, 'POST', this.dataForm, true, (resp) => {
                if (resp && (resp.code === 0 || resp.code === 200)) {
                    this.$message.success('操作成功');
                    this.visible = false;
                    this.$emit('refreshDataList');
                } else {
                    this.$message.error(resp.msg || '操作失败');
                }
            });
        }
    });
}
```

**核心代码 - 后端新增燃气用户接口（含户号唯一性校验）：**
```java
// gas-api/.../controller/GasUserController.java
@PostMapping("/save")
@SaCheckLogin
@SaCheckPermission(value = {"ROOT", "GAS_USER:INSERT"}, mode = SaMode.OR)
public R save(@RequestBody @Valid InsertGasUserForm form) {
    if (gasUserService.checkUserNoExists(form.getUserNo(), null)) {
        return R.error("用户户号已存在");
    }
    GasUserEntity entity = new GasUserEntity();
    BeanUtil.copyProperties(form, entity);
    entity.setStatus((byte) 1);
    entity.setDeleted(false);
    gasUserService.insert(entity);
    return R.ok();
}
```

---

## 八、巡检日程表

### 【新增】巡检日程

| 字段 | 输入值 |
|------|--------|
| 科室班组 | 巡检一科/巡检一组 |
| 巡检人员 | 张三 |
| 巡检用户 | 李四 |
| 巡检日期 | 2026-04-29（或当天日期） |
| 最大人数 | 5 |
| 备注 | 日常巡检 |

**操作步骤：**
1. 点击左侧菜单【巡检日程表】
2. 点击【新增】按钮
3. 选择科室班组：`巡检一科/巡检一组`
4. 选择巡检人员：`张三`
5. 选择巡检用户：`李四`
6. 设置巡检日期
7. 设置最大人数：`5`
8. 点击【确定】保存

> **涉及代码位置**
> - 前端日程页：`gas-vue/src/views/medical_dept_sub_work_plan.vue` → `addHandle()` / `loadDataList()` / `formatData()`
> - 前端弹窗页：`gas-vue/src/views/medical_dept_sub_work_plan-add.vue` → `dataFormSubmit()` / `loadActualCount()` / `loadExistingMaximum()`
> - 后端接口：`gas-api/.../controller/MedicalDeptSubWorkerPlanController.java` → `POST /worker_plan/insert`
> - 后端Service：`gas-api/.../service/MedicalDeptSubWorkerPlanService.java`
> - 后端Dao：`gas-api/.../db/dao/GasWorkerPlanDao.java` + `mapper/GasWorkerPlanDao.xml`

**核心代码 - 前端提交方法（含人数上限校验）：**
```javascript
// gas-vue/src/views/medical_dept_sub_work_plan-add.vue
dataFormSubmit: function() {
    let that = this;
    that.$refs['dataForm'].validate(valid => {
        if (valid) {
            if (that.actualCount >= that.dataForm.maximum) {
                ElMessage.error('当天该班组已达到最大人数，无法继续派遣');
                return;
            }
            let data = {
                deptSubId: that.dataForm.deptSubId,
                workerId: that.dataForm.workerId,
                userId: that.dataForm.userId,
                date: that.dataForm.date,
                maximum: that.dataForm.maximum,
                description: that.dataForm.description
            };
            that.$http('/worker_plan/insert', 'POST', data, true, function(resp) {
                if (resp.code === 0 || resp.code === 200) {
                    ElMessage.success('新增巡检计划成功');
                    that.visible = false;
                    that.$emit('refreshDataList');
                } else {
                    ElMessage.error(resp.msg || '新增失败');
                }
            });
        }
    });
}
```

**核心代码 - 后端新增巡检计划接口（含人数上限校验）：**
```java
// gas-api/.../controller/MedicalDeptSubWorkerPlanController.java
@PostMapping("/insert")
@SaCheckLogin
@SaCheckPermission(value = {"ROOT", "SCHEDULE:INSERT"}, mode = SaMode.OR)
public R insert(@RequestBody @Valid InsertGasWorkerPlanForm form) {
    Map<String, Object> param = new HashMap<>();
    param.put("deptSubId", form.getDeptSubId());
    param.put("date", form.getDate());
    int actualCount = medicalDeptSubWorkerPlanService.countByDateAndDeptSub(param);
    Integer existingMaximum = medicalDeptSubWorkerPlanService.searchMaximumByDateAndDeptSub(param);
    int maximum = form.getMaximum() != null ? form.getMaximum() : 1;
    if (existingMaximum != null && existingMaximum > 0) {
        maximum = existingMaximum;
    }
    if (actualCount >= maximum) {
        return R.error("当天该班组已达到最大人数(" + maximum + "人)，无法继续派遣");
    }
    GasWorkerPlanEntity entity = new GasWorkerPlanEntity();
    entity.setDeptSubId(form.getDeptSubId());
    entity.setWorkerId(form.getWorkerId());
    entity.setDate(DateUtil.parse(form.getDate()));
    entity.setUserId(form.getUserId());
    entity.setMaximum(maximum);
    entity.setDescription(form.getDescription());
    int result = medicalDeptSubWorkerPlanService.insert(entity);
    return result > 0 ? R.ok() : R.error("新增巡检计划失败");
}
```

---

## 九、燃气巡检计划

### 【新增】巡检计划

| 字段 | 输入值 |
|------|--------|
| 科室班组 | 巡检一科/巡检一组 |
| 巡检人员 | 张三 |
| 燃气用户 | 李四 |
| 巡检日期 | 2026-04-29 |
| 最大人数 | 5 |
| 备注 | 计划巡检 |

**操作步骤：**
1. 点击左侧菜单【燃气巡检计划】
2. 点击【新增】按钮
3. 填写以上信息
4. 点击【确定】保存

> **涉及代码位置**
> - 前端列表页：`gas-vue/src/views/gas_worker_plan.vue` → `addHandle()` / `getDataList()` / `generateReportHandle()`
> - 前端弹窗页：`gas-vue/src/views/gas_worker_plan-add-or-update.vue` → `dataFormSubmit()` / `loadDept()` / `loadActualCount()`
> - 后端接口：`gas-api/.../controller/MedicalDeptSubWorkerPlanController.java` → `POST /worker_plan/insert` / `POST /worker_plan/update`
> - 后端Service：`gas-api/.../service/MedicalDeptSubWorkerPlanService.java`
> - 后端Dao：`gas-api/.../db/dao/GasWorkerPlanDao.java` + `mapper/GasWorkerPlanDao.xml`
> - 巡检报告：`gas-api/.../controller/GasWorkerInspectionReportController.java` → `POST /inspection_report/generateAndUpload`

**核心代码 - 前端提交方法：**
```javascript
// gas-vue/src/views/gas_worker_plan-add-or-update.vue
dataFormSubmit: function() {
    let that = this;
    that.$refs['dataForm'].validate(valid => {
        if (valid) {
            let data = {
                id: that.dataForm.id,
                deptSubId: that.dataForm.deptSubId,
                workerId: that.dataForm.workerId,
                userId: that.dataForm.userId,
                date: that.dataForm.date,
                maximum: that.dataForm.maximum,
                description: that.dataForm.description
            };
            let url = that.dataForm.id ? '/worker_plan/update' : '/worker_plan/insert';
            that.$http(url, 'POST', data, true, function(resp) {
                if (resp.code === 0 || resp.code === 200) {
                    ElMessage.success(that.dataForm.id ? '更新燃气巡检计划成功' : '新增燃气巡检计划成功');
                    that.visible = false;
                    that.$emit('refreshDataList');
                } else {
                    ElMessage.error(resp.msg || '操作失败');
                }
            });
        }
    });
}
```

---

## 十、隐患管理

### 【新增】隐患信息

| 字段 | 输入值 |
|------|--------|
| 隐患分类 | 上报 |
| 隐患等级 | 一般 |
| 隐患来源 | 管网 |
| 关联用户 | 李四 |
| 行政区域 | 重庆市/梁平区/梁山街道 |
| 详细地址 | 人民路100号 |
| 隐患描述 | 管道接口处有轻微漏气 |
| 整改开始日期 | 2026-04-29 |
| 整改结束日期 | 2026-05-05 |
| 提交人姓名 | 张三 |
| 指派人 | 张三 |
| 是否本人整改 | 是 |
| 整改状态 | 待整改 |

**操作步骤：**
1. 点击左侧菜单【隐患管理】
2. 点击【新增】按钮
3. 填写以上信息
4. 可上传隐患图片
5. 点击【确定】保存

> **涉及代码位置**
> - 前端列表页：`gas-vue/src/views/hazard_info.vue` → `addHandle()` / `getDataList()`
> - 前端弹窗页：`gas-vue/src/views/hazard_info-add-or-update.vue` → `dataFormSubmit()` / `loadAreaData()` / `loadUserList()` / `loadWorkerList()` / `openMapSelector()` / `handleUploadSuccess()`
> - 后端接口：`gas-api/.../controller/GasHazardInfoController.java` → `POST /hazard_info/save` + `POST /hazard_info/uploadPhoto`
> - 后端Service：`gas-api/.../service/GasHazardInfoService.java`
> - 后端Dao：`gas-api/.../db/dao/GasHazardInfoDao.java` + `mapper/GasHazardInfoDao.xml`
> - 图片上传：MinIO对象存储，桶名 `hidden-danger`

**核心代码 - 前端提交方法：**
```javascript
// gas-vue/src/views/hazard_info-add-or-update.vue
dataFormSubmit() {
    this.$refs.dataForm.validate(valid => {
        if (valid) {
            const url = this.dataForm.id ? '/hazard_info/update' : '/hazard_info/save';
            this.$http(url, 'POST', this.dataForm, true, (resp) => {
                if (resp && (resp.code === 0 || resp.code === 200)) {
                    this.$message.success('操作成功');
                    this.visible = false;
                    this.$emit('refreshDataList');
                } else {
                    this.$message.error(resp.msg || '操作失败');
                }
            });
        }
    });
}
```

**核心代码 - 后端新增隐患接口（含经纬度、日期等字段类型转换）：**
```java
// gas-api/.../controller/GasHazardInfoController.java
@PostMapping("/save")
@SaCheckLogin
@SaCheckPermission(value = {"ROOT", "HAZARD_INFO:INSERT"}, mode = SaMode.OR)
public R save(@RequestBody Map<String, Object> params) {
    GasHazardInfoEntity entity = new GasHazardInfoEntity();
    BeanUtil.copyProperties(params, entity);
    if (params.get("longitude") != null) {
        entity.setLongitude(new BigDecimal(params.get("longitude").toString()));
    }
    if (params.get("latitude") != null) {
        entity.setLatitude(new BigDecimal(params.get("latitude").toString()));
    }
    if (params.get("userId") != null && !params.get("userId").toString().isEmpty()) {
        entity.setUserId(Integer.parseInt(params.get("userId").toString()));
    }
    if (params.get("assigneeId") != null && !params.get("assigneeId").toString().isEmpty()) {
        entity.setAssigneeId(Integer.parseInt(params.get("assigneeId").toString()));
    }
    entity.setSubmitTime(new Date());
    gasHazardInfoService.insert(entity);
    return R.ok();
}
```

**核心代码 - 后端上传隐患图片到MinIO：**
```java
// gas-api/.../controller/GasHazardInfoController.java
@PostMapping("/uploadPhoto")
@SaCheckLogin
public R uploadPhoto(@RequestParam("file") MultipartFile file,
                     @RequestParam(value = "hazardId", required = false) Integer hazardId) {
    if (file.isEmpty()) { return R.error().put("msg", "文件不能为空"); }
    String suffix = file.getOriginalFilename().substring(file.getOriginalFilename().lastIndexOf("."));
    if (!suffix.equalsIgnoreCase(".jpg") && !suffix.equalsIgnoreCase(".jpeg") && !suffix.equalsIgnoreCase(".png")) {
        return R.error().put("msg", "只支持JPG、JPEG、PNG格式的图片");
    }
    String filename = UUID.randomUUID().toString() + suffix;
    String objectName = hazardId != null 
        ? "/photo/hazard/" + hazardId + "/" + filename 
        : "/photo/hazard/temp/" + filename;
    MinioClient client = MinioClient.builder().endpoint(endpoint).credentials(accessKey, secretKey).build();
    client.putObject(PutObjectArgs.builder()
        .bucket("hidden-danger").object(objectName)
        .stream(file.getInputStream(), -1, 5 * 1024 * 1024)
        .contentType(file.getContentType()).build());
    return R.ok().put("result", Map.of("photo", objectName, "url", objectName, "path", objectName));
}
```

---

# 第二部分：微信小程序

---

## 一、微信小程序登录注册

### 【登录】微信小程序

**操作步骤：**
1. 打开微信，搜索小程序或扫码进入
2. 点击底部【个人中心】
3. 点击【注册/登录】
4. 点击【允许】微信授权
5. 显示"登录成功"或"注册成功"

**⚠️ 说明：** 首次授权自动注册，后续授权自动登录。

> **涉及代码位置**
> - 前端页面：`patient-wx/pages/mine/mine.vue` → `loginOrRegister()` 方法
> - 后端接口：`patient-wx-api/.../controller/UserController.java` → `POST /user/loginOrRegister`
> - 后端Service：`patient-wx-api/.../service/UserService.java`
> - 后端Dao：`patient-wx-api/.../db/dao/UserDao.java` + `mapper/UserDao.xml`
> - 认证框架：Sa-Token `StpUtil.login(id)`

**核心代码 - 前端微信登录注册方法：**
```javascript
// patient-wx/pages/mine/mine.vue
loginOrRegister: function() {
    let that = this;
    uni.login({
        provider: 'weixin',
        success: function(resp) {
            let code = resp.code;
            that.code = code;
        }
    });
    uni.getUserProfile({
        desc: '获取用户信息',
        success: function(resp) {
            let info = resp.userInfo;
            let nickname = info.nickName;
            let photo = info.avatarUrl;
            let sex = info.gender == 0 ? '男' : '女';
            let data = { code: that.code, nickname: nickname, photo: photo, sex: sex };
            that.ajax(that.api.loginOrRegister, 'POST', data, function(resp) {
                let msg = resp.data.msg;
                that.$refs.uToast.show({
                    message: msg,
                    type: 'success',
                    duration: 1200,
                    complete: function() {
                        let token = resp.data.token;
                        uni.setStorageSync('token', token);
                        that.flag = 'login';
                        that.user.username = nickname || '微信用户';
                        that.user.photo = photo || '';
                        if (resp.data.hasOwnProperty('tel')) {
                            that.user.tel = resp.data.tel;
                        }
                    }
                });
            });
        }
    });
}
```

**核心代码 - 后端微信登录注册接口：**
```java
// patient-wx-api/.../controller/UserController.java
@PostMapping("/loginOrRegister")
public R loginOrRegister(@RequestBody @Valid LoginOrRegisterForm form) {
    HashMap map = userService.loginOrRegister(form.getCode(), form.getNickname(), form.getPhoto(), form.getSex());
    String msg = MapUtil.getStr(map, "msg");
    Integer id = MapUtil.getInt(map, "id");
    StpUtil.login(id);
    String token = StpUtil.getTokenValue();
    R r = R.ok(msg).put("token", token);
    if (map.containsKey("tel")) {
        r.put("tel", MapUtil.getStr(map, "tel"));
    }
    return r;
}
```

---

## 二、实名登记（绑定巡检人员）

### 【新增】实名信息

| 字段 | 输入值 |
|------|--------|
| 姓名 | 张三 |
| 性别 | 男 |
| 身份证号 | 500235199001010001 |
| 手机号码 | 13900139000 |
| 出生日期 | 1990-01-01 |

**操作步骤：**
1. 点击底部【个人中心】
2. 点击【实名登记】
3. 填写姓名：`张三`
4. 选择性别：`男`
5. 填写身份证号：`500235199001010001`
6. 填写手机号码：`13900139000`
7. 选择出生日期：`1990-01-01`
8. 点击【提交信息】
9. 确认弹窗点击【确定】

> **涉及代码位置**
> - 前端页面：`patient-wx/user/fill_user_info/fill_user_info.vue` → `submitHandle()` / `nextHandle()` / `scanIdcardFront()`
> - 后端接口：`patient-wx-api/.../controller/UserInfoCardController.java` → `POST /user/info/card/insert`
> - 后端Service：`patient-wx-api/.../service/UserInfoCardService.java`
> - 后端Dao：`patient-wx-api/.../db/dao/UserInfoCardDao.java`

**核心代码 - 前端提交方法：**
```javascript
// patient-wx/user/fill_user_info/fill_user_info.vue
submitHandle: function() {
    let that = this;
    uni.showModal({
        title: '提示消息',
        content: that.flag == 'insert' ? '确定创建？' : '确定更新信息？',
        success: function(resp) {
            if (resp.confirm) {
                let data = that.dataForm;
                let url = that.flag == 'insert' ? that.api.insertUserInfoCard : that.api.updateUserInfoCard;
                that.ajax(url, 'POST', data, function(resp) {
                    uni.showToast({
                        icon: 'success',
                        title: that.flag == 'insert' ? '创建成功' : '更新成功'
                    });
                    setTimeout(function() {
                        uni.switchTab({ url: '/pages/mine/mine' });
                    }, 1500);
                });
            }
        }
    });
}
```

**核心代码 - 后端新增用户信息卡接口：**
```java
// patient-wx-api/.../controller/UserInfoCardController.java
@PostMapping("/insert")
public R insert(@RequestBody @Valid InsertUserInfoCardForm form) {
    UserInfoCardEntity entity = new UserInfoCardEntity();
    entity.setName(form.getName());
    entity.setSex(form.getSex());
    entity.setPid(form.getPid());
    entity.setTel(form.getTel());
    entity.setBirthday(form.getBirthday());
    entity.setInsuranceType(form.getInsuranceType());
    int userId = StpUtil.getLoginIdAsInt();
    entity.setUserId(userId);
    entity.setUuid(IdUtil.simpleUUID());
    String json = JSONUtil.parseArray(form.getMedicalHistory()).toString();
    entity.setMedicalHistory(json);
    userInfoCardService.insert(entity);
    return R.ok();
}
```

---

## 三、绑定巡检人员（后台操作）

### 【修改】用户信息卡 - 绑定巡检人员

**⚠️ 此步骤需要在Web管理后台操作或数据库操作：**

```sql
-- 查询微信用户ID
SELECT id, nickname FROM patient_user;

-- 查询巡检人员ID
SELECT id, name, tel FROM gas_worker WHERE name = '张三';

-- 绑定巡检人员（将 user_id 和 worker_id 替换为实际值）
UPDATE patient_user_info_card 
SET worker_id = 3 
WHERE user_id = 7;
```

**验证绑定成功：**
- 微信小程序【个人中心】→【我的信息】能看到巡检人员详情

> **涉及代码位置**
> - 前端我的信息页：`patient-wx/user/gas_worker_info/gas_worker_info.vue` → `onLoad()`
> - 后端查询巡检员信息：`patient-wx-api/.../controller/GasWorkerController.java` → `POST /worker/searchContent`
> - 后端查询用户信息卡：`patient-wx-api/.../controller/UserInfoCardController.java` → `GET /user/info/card/searchUserInfoCard`
> - 数据库表：`patient_user_info_card`（`worker_id` 字段关联 `gas_worker` 表）

**核心代码 - 后端查询巡检员信息接口：**
```java
// patient-wx-api/.../controller/GasWorkerController.java
@PostMapping("/searchContent")
public R searchContent() {
    int userId = StpUtil.getLoginIdAsInt();
    HashMap map = gasWorkerService.searchContentByUserId(userId);
    return R.ok().put("result", map);
}
```

**核心代码 - 后端查询用户信息卡接口：**
```java
// patient-wx-api/.../controller/UserInfoCardController.java
@GetMapping("/searchUserInfoCard")
public R searchUserInfoCard() {
    int userId = StpUtil.getLoginIdAsInt();
    HashMap map = userInfoCardService.searchUserInfoCard(userId);
    return R.ok().put("result", map);
}
```

---

## 四、查看我的任务

### 【查询】巡检计划

**操作步骤：**
1. 点击底部【个人中心】
2. 点击【我的任务】
3. 查看日期列表，有巡检任务的日期显示"巡检"
4. 点击日期查看当日巡检计划详情

> **涉及代码位置**
> - 前端任务页：`patient-wx/registration/worker_schedule/worker_plan.vue` → `searchCanWorkInDateRange()` / `searchWorkerPlanInDay()` / `clickBtnHandle()`
> - 后端接口：`patient-wx-api/.../controller/GasWorkerPlanController.java` → `POST /worker_plan/searchCanWorkInDateRange` + `POST /worker_plan/searchWorkerPlanInDay`
> - 后端Service：`patient-wx-api/.../service/GasWorkerPlanService.java`
> - 后端Dao：`patient-wx-api/.../db/dao/GasWorkerPlanDao.java`

**核心代码 - 前端查询日期范围内可工作计划：**
```javascript
// patient-wx/registration/worker_schedule/worker_plan.vue
searchCanWorkInDateRange: function(ref) {
    let startDate = dayjs().format('YYYY-MM-DD');
    let endDate = dayjs().add(6, 'day').format('YYYY-MM-DD');
    let data = { startDate: startDate, endDate: endDate };
    ref.ajax(ref.api.searchCanWorkInDateRange, 'POST', data,
        function(resp) {
            let result = resp.data.result;
            let array = ['日', '一', '二', '三', '四', '五', '六'];
            for (let one of result) {
                let day = array[dayjs(one.date).day()];
                one.day = day;
                one.dateOfMonth = dayjs(one.date).date();
            }
            ref.dateList = result;
        }, false
    );
}
```

**核心代码 - 后端查询巡检计划接口（需绑定巡检员）：**
```java
// patient-wx-api/.../controller/GasWorkerPlanController.java
@PostMapping("/searchCanWorkInDateRange")
@SaCheckLogin
public R searchCanWorkInDateRange(@RequestBody @Valid SearchCanWorkInDateRangeForm form) {
    int userId = StpUtil.getLoginIdAsInt();
    UserInfoCardEntity card = userInfoCardDao.searchByUserId(userId);
    if (card == null || card.getWorkerId() == null) {
        return R.ok().put("result", new ArrayList<>());
    }
    Map<String, Object> param = BeanUtil.beanToMap(form);
    param.put("workerId", card.getWorkerId());
    ArrayList<HashMap> list = gasWorkerPlanService.searchCanWorkInDateRange(param);
    return R.ok().put("result", list);
}
```

---

## 五、执行巡检

### 【新增】巡检记录

**操作步骤：**
1. 在【我的任务】页面选择有巡检计划的日期
2. 点击巡检计划右侧【巡检】按钮
3. 进入巡检页面，填写：
   - 巡检结果
   - 是否有隐患
   - 巡检描述
   - 上传照片
4. 点击【提交】

> **涉及代码位置**
> - 前端巡检页：`patient-wx/registration/worker_inspection/worker_inspection.vue` → `submitInspection()` / `loadExistingInspection()` / `choosePhoto()` / `uploadPhoto()`
> - 后端接口：`patient-wx-api/.../controller/GasWorkerInspectionController.java` → `POST /worker_inspection/save` + `POST /worker_inspection/update`
> - 后端Service：`patient-wx-api/.../service/GasWorkerInspectionService.java`
> - 后端Dao：`patient-wx-api/.../db/dao/GasWorkerInspectionDao.java`
> - 照片上传：`patient-wx-api/.../controller/UploadController.java` → `POST /upload/inspectionPhoto`

**核心代码 - 前端提交巡检结果方法：**
```javascript
// patient-wx/registration/worker_inspection/worker_inspection.vue
submitInspection() {
    let that = this;
    let result = that.resultOptions[that.resultIndex].value;
    if (result === '完成巡检') {
        for (let i = 0; i < that.inspectItems.length; i++) {
            let item = that.inspectItems[i];
            if (item.hasDanger === 0 && !item.desc.trim()) {
                uni.showToast({ icon: 'none', title: `请填写${item.name}的整改措施` });
                return;
            }
        }
    }
    let data = {
        workerPlanId: that.workerPlanId,
        hasDanger: that.hasDanger,
        inspectDesc: that.allDesc,
        inspectPhoto: that.allPhotos,
        inspectResult: result
    };
    let url = that.existingId ? that.api.updateInspection : that.api.saveInspection;
    if (that.existingId) { data.id = that.existingId; }
    that.ajax(url, 'POST', data, function(resp) {
        uni.showToast({ icon: 'success', title: '提交成功' });
        setTimeout(() => { uni.navigateBack(); }, 1500);
    }, true);
}
```

**核心代码 - 后端保存巡检记录接口（含upsert逻辑）：**
```java
// patient-wx-api/.../controller/GasWorkerInspectionController.java
@PostMapping("/save")
@SaCheckLogin
public R save(@RequestBody @Valid SaveInspectionForm form) {
    int userId = StpUtil.getLoginIdAsInt();
    UserInfoCardEntity card = userInfoCardDao.searchByUserId(userId);
    if (card == null || card.getWorkerId() == null) {
        return R.error("未绑定巡检员信息");
    }
    HashMap existing = gasWorkerInspectionService.searchByWorkerPlanId(form.getWorkerPlanId());
    GasWorkerInspectionEntity entity = new GasWorkerInspectionEntity();
    entity.setWorkerPlanId(form.getWorkerPlanId());
    entity.setWorkerId(card.getWorkerId());
    entity.setInspectDate(new Date());
    entity.setHasDanger(form.getHasDanger());
    entity.setInspectDesc(form.getInspectDesc());
    entity.setInspectPhoto(form.getInspectPhoto());
    entity.setInspectResult(form.getInspectResult());
    if (existing != null && existing.get("id") != null) {
        entity.setId(((Number) existing.get("id")).intValue());
        gasWorkerInspectionService.update(entity);
    } else {
        gasWorkerInspectionService.insert(entity);
    }
    return R.ok();
}
```

---

## 六、隐患整改

### 【修改】隐患整改

**操作步骤：**
1. 点击底部【个人中心】
2. 点击【隐患整改】
3. 查看待整改的隐患列表
4. 点击隐患进入详情页
5. 点击【提交整改】
6. 上传整改照片/视频
7. 填写整改说明
8. 点击【提交】
9. 整改状态变为"已整改"

> **涉及代码位置**
> - 前端隐患列表页：`patient-wx/registration/notice/notice.vue`（隐患整改入口在mine.vue中）
> - 后端查询隐患列表：`patient-wx-api/.../controller/GasHazardInfoController.java` → `POST /hazard_info/wx/searchByAssigneeId`
> - 后端提交整改：`patient-wx-api/.../controller/GasHazardInfoController.java` → `POST /hazard_info/wx/submitRectification`
> - 后端上传整改图片：`patient-wx-api/.../controller/GasHazardInfoController.java` → `POST /hazard_info/wx/uploadRectificationPhoto`
> - 后端上传整改视频：`patient-wx-api/.../controller/GasHazardInfoController.java` → `POST /hazard_info/wx/uploadRectificationVideo`
> - 后端Service：`patient-wx-api/.../service/GasHazardInfoService.java`
> - 后端Dao：`patient-wx-api/.../db/dao/GasHazardInfoDao.java`
> - 文件存储：MinIO，桶名 `rectification`

**核心代码 - 后端按指派人查询隐患列表：**
```java
// patient-wx-api/.../controller/GasHazardInfoController.java
@PostMapping("/wx/searchByAssigneeId")
public R wxSearchByAssigneeId(@RequestBody Map<String, Object> params) {
    Integer assigneeId = params.get("assigneeId") != null ? Integer.parseInt(params.get("assigneeId").toString()) : null;
    Integer rectifyStatus = params.get("rectifyStatus") != null ? Integer.parseInt(params.get("rectifyStatus").toString()) : null;
    if (assigneeId == null) { return R.error().put("msg", "指派人ID不能为空"); }
    int page = params.get("page") != null ? Integer.parseInt(params.get("page").toString()) : 1;
    int length = params.get("length") != null ? Integer.parseInt(params.get("length").toString()) : 10;
    int start = (page - 1) * length;
    Map<String, Object> param = new HashMap<>();
    param.put("assigneeId", assigneeId);
    param.put("rectifyStatus", rectifyStatus);
    param.put("start", start);
    param.put("length", length);
    PageUtils pageUtils = gasHazardInfoService.searchByAssigneeId(param);
    return R.ok().put("result", pageUtils);
}
```

**核心代码 - 后端提交整改接口：**
```java
// patient-wx-api/.../controller/GasHazardInfoController.java
@PostMapping("/wx/submitRectification")
public R wxSubmitRectification(@RequestBody Map<String, Object> params) {
    Integer id = params.get("id") != null ? Integer.parseInt(params.get("id").toString()) : null;
    if (id == null) { return R.error().put("msg", "隐患ID不能为空"); }
    Map<String, Object> param = new HashMap<>();
    param.put("id", id);
    param.put("rectifyStatus", 1);
    param.put("rectifyTime", new Date());
    if (params.get("rectifyPhoto") != null) { param.put("rectifyPhoto", params.get("rectifyPhoto").toString()); }
    if (params.get("rectifyVideo") != null) { param.put("rectifyVideo", params.get("rectifyVideo").toString()); }
    gasHazardInfoService.update(param);
    return R.ok().put("msg", "整改提交成功");
}
```

**核心代码 - 后端上传整改图片到MinIO：**
```java
// patient-wx-api/.../controller/GasHazardInfoController.java
@PostMapping("/wx/uploadRectificationPhoto")
public R wxUploadRectificationPhoto(@RequestParam("file") MultipartFile file,
                                    @RequestParam("hazardId") Integer hazardId,
                                    @RequestParam(value = "index", required = false) Integer index) {
    if (file.isEmpty()) { return R.error().put("msg", "文件不能为空"); }
    String suffix = file.getOriginalFilename().substring(file.getOriginalFilename().lastIndexOf("."));
    if (!suffix.equalsIgnoreCase(".jpg") && !suffix.equalsIgnoreCase(".jpeg") && !suffix.equalsIgnoreCase(".png")) {
        return R.error().put("msg", "只支持JPG、JPEG、PNG格式的图片");
    }
    String filename = "photo_" + (index != null ? index : System.currentTimeMillis()) + suffix;
    String objectName = "/" + hazardId + "/" + filename;
    MinioClient client = MinioClient.builder().endpoint(endpoint).credentials(accessKey, secretKey).build();
    client.putObject(PutObjectArgs.builder()
        .bucket("rectification").object(objectName)
        .stream(file.getInputStream(), -1, 10 * 1024 * 1024)
        .contentType(file.getContentType()).build());
    return R.ok().put("result", Map.of("path", objectName));
}
```

---

# 第三部分：Web管理后台验证

---

## 一、登录验证

### 【登录】Web管理后台

```
用户名：admin
密码：abc123456
```

> **涉及代码位置**（同第一部分登录，此处略）

---

## 二、查看巡检记录

### 【查询】巡检记录

**操作步骤：**
1. 点击左侧菜单【燃气巡检计划】
2. 查看巡检记录列表
3. 验证微信小程序提交的巡检数据已同步显示

> **涉及代码位置**
> - 前端列表页：`gas-vue/src/views/gas_worker_plan.vue` → `getDataList()`
> - 后端接口：`gas-api/.../controller/MedicalDeptSubWorkerPlanController.java` → `POST /worker_plan/searchByPage`
> - 巡检记录查询：`gas-api/.../controller/GasWorkerInspectionReportController.java` → `POST /inspection_report/exportByPlanId`

**核心代码 - 后端分页查询巡检计划接口：**
```java
// gas-api/.../controller/MedicalDeptSubWorkerPlanController.java
@PostMapping("/searchByPage")
@SaCheckLogin
@SaCheckPermission(value = {"ROOT", "SCHEDULE:SELECT"}, mode = SaMode.OR)
public R searchByPage(@RequestBody @Valid SearchGasWorkerPlanByPageForm form) {
    Map param = BeanUtil.beanToMap(form);
    int page = form.getPage();
    int length = form.getLength();
    int start = (page - 1) * length;
    param.put("start", start);
    PageUtils pageUtils = medicalDeptSubWorkerPlanService.searchByPage(param);
    return R.ok().put("result", pageUtils);
}
```

---

## 三、查看隐患整改

### 【查询】隐患整改状态

**操作步骤：**
1. 点击左侧菜单【隐患管理】
2. 查看隐患列表
3. 验证整改状态已更新为"已整改"
4. 查看整改照片、整改时间等信息

> **涉及代码位置**
> - 前端列表页：`gas-vue/src/views/hazard_info.vue` → `getDataList()`
> - 后端接口：`gas-api/.../controller/GasHazardInfoController.java` → `POST /hazard_info/searchByPage`
> - 后端Service：`gas-api/.../service/GasHazardInfoService.java`
> - 后端Dao：`gas-api/.../db/dao/GasHazardInfoDao.java` + `mapper/GasHazardInfoDao.xml`

**核心代码 - 后端分页查询隐患接口：**
```java
// gas-api/.../controller/GasHazardInfoController.java
@PostMapping("/searchByPage")
@SaCheckLogin
@SaCheckPermission(value = {"ROOT", "HAZARD_INFO:SELECT"}, mode = SaMode.OR)
public R searchByPage(@RequestBody Map<String, Object> params) {
    int page = params.get("page") != null ? Integer.parseInt(params.get("page").toString()) : 1;
    int length = params.get("length") != null ? Integer.parseInt(params.get("length").toString()) : 10;
    int start = (page - 1) * length;
    params.put("start", start);
    PageUtils pageUtils = gasHazardInfoService.searchByPage(params);
    return R.ok().put("result", pageUtils);
}
```

---

# 第四部分：功能检查清单

## Web管理后台

| 序号 | 功能模块 | 操作类型 | 涉及前端页面 | 涉及后端Controller | 涉及API端点 | 测试结果 |
|------|----------|----------|-------------|-------------------|------------|----------|
| 1 | 部门管理 | 新增 | `dept-add-or-update.vue` | `MisDeptController` | `POST /mis_dept/save` | □ 通过 □ 失败 |
| 2 | 科室管理 | 新增 | `medical_dept-add-or-update.vue` | `MedicalDeptController` | `GET /medical_dept/searchAll` | □ 通过 □ 失败 |
| 3 | 班组管理 | 新增 | `medical_dept_sub-add-or-update.vue` | `MedicalDeptSubController` | `GET /medical_dept_sub/searchByDeptId` | □ 通过 □ 失败 |
| 4 | 角色管理 | 新增 | `role-add-or-update.vue` | `MisRoleController` | `POST /role/save` + `POST /role/save-permissions` | □ 通过 □ 失败 |
| 5 | 用户管理 | 新增 | `user-add-or-update.vue` | `MisUserController` | `POST /mis_user/save` + `POST /mis_user/save-roles` | □ 通过 □ 失败 |
| 6 | 用户管理 | 登录验证 | `login.vue` | `MisUserController` | `POST /mis_user/login` | □ 通过 □ 失败 |
| 7 | 巡检人员管理 | 新增 | `worker-add-or-update.vue` | `GasWorkerController` | `POST /gas_worker/save` | □ 通过 □ 失败 |
| 8 | 燃气用户管理 | 新增 | `gas_user-add-or-update.vue` | `GasUserController` | `POST /gas_user/save` | □ 通过 □ 失败 |
| 9 | 巡检日程表 | 新增 | `medical_dept_sub_work_plan-add.vue` | `MedicalDeptSubWorkerPlanController` | `POST /worker_plan/insert` | □ 通过 □ 失败 |
| 10 | 燃气巡检计划 | 新增 | `gas_worker_plan-add-or-update.vue` | `MedicalDeptSubWorkerPlanController` | `POST /worker_plan/insert` | □ 通过 □ 失败 |
| 11 | 隐患管理 | 新增 | `hazard_info-add-or-update.vue` | `GasHazardInfoController` | `POST /hazard_info/save` | □ 通过 □ 失败 |

## 微信小程序

| 序号 | 功能模块 | 操作类型 | 涉及前端页面 | 涉及后端Controller | 涉及API端点 | 测试结果 |
|------|----------|----------|-------------|-------------------|------------|----------|
| 1 | 登录注册 | 登录/注册 | `mine.vue` | `UserController` | `POST /user/loginOrRegister` | □ 通过 □ 失败 |
| 2 | 实名登记 | 新增 | `fill_user_info.vue` | `UserInfoCardController` | `POST /user/info/card/insert` | □ 通过 □ 失败 |
| 3 | 我的信息 | 查询 | `gas_worker_info.vue` | `GasWorkerController` | `POST /worker/searchContent` | □ 通过 □ 失败 |
| 4 | 我的任务 | 查询 | `worker_plan.vue` | `GasWorkerPlanController` | `POST /worker_plan/searchCanWorkInDateRange` | □ 通过 □ 失败 |
| 5 | 执行巡检 | 新增 | `worker_inspection.vue` | `GasWorkerInspectionController` | `POST /worker_inspection/save` | □ 通过 □ 失败 |
| 6 | 隐患整改 | 修改 | `mine.vue`(入口) | `GasHazardInfoController` | `POST /hazard_info/wx/submitRectification` | □ 通过 □ 失败 |

---

# 第五部分：项目代码架构总览

## 后端模块

| 模块 | 包路径 | 端口 | 用途 |
|------|--------|------|------|
| gas-api | `com.example.gas.api` | 8080 | Web管理后台API |
| gas-admin-api | `com.example.gas.admin.api` | 8081 | 管理后台API（备用） |
| patient-wx-api | `com.example.gas.patient.wx.api` | 8082 | 微信小程序API |

## 前端模块

| 模块 | 框架 | 用途 |
|------|------|------|
| gas-vue | Vue 3 + Element Plus + Vite | Web管理后台 |
| patient-wx | uni-app + uView UI | 微信小程序 |

## 技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Spring Boot | 2.7.x | 后端框架 |
| Sa-Token | - | 认证鉴权 |
| MyBatis | - | ORM框架 |
| MySQL | 5.7 | 关系型数据库 |
| Redis | 6.0 | 缓存 |
| MinIO | - | 对象存储（图片/视频/报告） |
| 高德地图API | - | 地图选点、地理编码 |

---

# 第六部分：测试环境

- 操作系统：Windows 10/11
- 浏览器：Chrome 100+ / Edge 100+
- 后端服务：Spring Boot 2.7.x
- 数据库：MySQL 5.7
- 缓存：Redis 6.0
- 对象存储：MinIO

---

**文档结束**
